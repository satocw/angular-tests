import { CustomAsSyntaxDirective } from './custom-as-syntax.directive';

describe('CustomAsSyntaxDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomAsSyntaxDirective();
    expect(directive).toBeTruthy();
  });
});
